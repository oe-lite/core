#@TYPE: Distribution
#@NAME: core
#@DESCRIPTION: Core OE-lite distro for toolchain-only purposes

MACHINE_OS = "linux-gnu"

GCC_VERSION	?= "4.3.2"
GLIBC_VERSION	?= "2.9"
TOOLCHAIN	 = "gcc-${GCC_VERSION}-glibc-${GLIBC_VERSION}"

PREFERRED_PROVIDER_${SDK_ARCH}-toolchain		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-dev		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-doc		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-local		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-garbage		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libc		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libdl		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libnss-files	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libnss-dns	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libresolv	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libutil		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libcrypt		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libpthread	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libm		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-librt		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libstdc++	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-machine-libgcc		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-dev			= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-doc			= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-local		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-garbage		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libc			= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libdl		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libnss-files		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libnss-dns		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libresolv		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libutil		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libcrypt		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libpthread		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libm			= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-librt		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libstdc++		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${SDK_ARCH}-sdk-libgcc		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-toolchain		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-dev		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-doc		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-local	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-garbage	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libc		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libdl	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libnss-files	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libnss-dns	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libresolv	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libutil	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libcrypt	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libpthread	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libm		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-librt	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libstdc++	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-machine-libgcc	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-dev		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-doc		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-local		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-garbage		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libc		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libdl		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libnss-files	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libnss-dns	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libresolv	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libutil		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libcrypt		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libpthread	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libm		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-librt		= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libstdc++	= "${TOOLCHAIN}"
PREFERRED_PROVIDER_${MACHINE_ARCH}-sdk-libgcc		= "${TOOLCHAIN}"
